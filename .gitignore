# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
.venv-windows/
.env
.secret_key
.secrets.json
teacher_model/

# Node
node_modules/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Generated docs (Marp → PDF; source .md is tracked, PDF is not)
docs/**/*.pdf

# ── Project data (all user data, never track) ──
projects/
# ...but the UI source folder of the same name is code, not data
!apps/ui/src/components/projects/

# ── Runtime directories ──
state/
storage/
logs/

# Internal development tools (not for OSS distribution)
scripts/development_tools/

# Script outputs
scripts/oneshot/*.log

# Model checkpoints (large binary files)
models/
*.pt
*.pth
# Temp/dev artifacts
pytest_out.txt
*.bundle
tmp_*.png
test-results/
260302_bestmodel/
*.db

# Debug screenshots & one-off images
debug_screenshots/
apps/ui/debug_screenshots/
gui_*.png
debug_*.png
architecture_diagram.html

_benchmark_cache/
val_predictions_*/

# ── E2E test outputs (keep specs, exclude artifacts) ──
apps/ui/e2e/screenshots/
apps/ui/e2e-results/
apps/ui/e2e/_archive/
apps/ui/e2e/E2E_REDESIGN.md

apps/ui/e2e/E2E_TEST_PLAN.md
apps/ui/e2e/UX_COVERAGE_EXPANSION*.md

.venv-macos/

# ── SBOM artifacts (release-scoped, generated by CI; not version-controlled) ──
# Local sanity-check outputs from cyclonedx-py / syft.
# Per-release SBOMs are attached to GitHub Releases by .github/workflows/sbom.yml.
sbom*.json
sbom*.spdx*
*.cdx.json
*.spdx.json

# Local-only benchmark / probe scripts — kept in working tree but not in the OSS repo.
# (Restored on 2026-07-01 after they were accidentally included by `git add -A`.)
scripts/autoconfig_probe.py
scripts/benchmark_accuracy.py
scripts/benchmark_accuracy_eval.py
scripts/benchmark_autoconfig.py
scripts/prepare_crack_dataset.py
# Local-only Playwright verify scripts (not part of the test suite)
apps/ui/verify_*.mjs
